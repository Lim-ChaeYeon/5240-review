# 팀 연도 결산 페이지 - 빠른 요약

## 🎯 프로젝트 요약

**목표**: Microsoft Edge 2025 결산 페이지 벤치마킹  
**개발 시간**: 1일 (2025-12-25)  
**최종 버전**: v2.1.0

---

## ✨ 주요 기능 (6가지)

1. ✅ **화면 클릭으로 슬라이드 이동**
2. ✅ **둥둥 떠다니는 아이콘 효과** (15개)
3. ✅ **마우스 반응 인트로 이미지** (패럴랙스)
4. ✅ **월별 대표 이미지 자동 배치** (1-3개, 8가지 위치)
5. ✅ **동영상 자동 재생**
6. ✅ **마지막 슬라이드에서 클릭으로만 완료**

---

## 🛠️ 기술 스택

| 항목 | 선택 |
|------|------|
| 슬라이드 | Swiper.js 11.x |
| 애니메이션 | AOS 2.3.x |
| 카운트업 | CountUp.js 2.8.x |
| 스타일 | 바닐라 CSS3 |
| 스크립트 | 바닐라 JS (ES6+) |

---

## 📦 파일 구조

```
review/ (84KB)
├── index.html       (5.3 KB) ⭐
├── style.css        (20 KB)  ⭐
├── app.js           (20 KB)  ⭐
├── data.js          (11 KB)  ⭐ 여기만 수정!
├── README.md        (9.0 KB)
├── CHANGELOG.md     (2.5 KB)
├── 개발과정.md      (35 KB)
└── 요약.md          (이 파일)
```

---

## 🚀 빠른 시작

### 1. 로컬 서버 실행
```bash
cd review
python3 -m http.server 8000
```

### 2. 브라우저 접속
```
http://localhost:8000
```

### 3. 데이터 수정
```javascript
// data.js 파일 열기
const teamYearData = [
  {
    month: '1월',
    title: '새로운 시작',
    stat: '5명',
    icon: '👥',
    images: ['이미지URL1', '이미지URL2'],
    video: 'https://동영상URL.mp4'
  }
];
```

---

## 🎨 버전별 주요 변경사항

### v1.0.0 (기본)
- Microsoft Edge 스타일 슬라이드
- 12개월 데이터 표시
- 키보드/휠/터치 지원

### v2.0.0 (인터랙티브)
- 화면 클릭 기능
- 떠다니는 아이콘 (15개)
- 마우스 반응 이미지
- 월별 이미지 배치 (8가지 패턴)
- 동영상 재생

### v2.1.0 (UX 개선)
- 마지막 슬라이드 클릭으로만 완료
- "클릭하여 결산 완료" 힌트 추가
- 사용자 제어권 강화

---

## 💡 핵심 코드

### 마우스 반응 (패럴랙스)
```javascript
document.addEventListener('mousemove', (e) => {
    mouseX = (e.clientX / window.innerWidth - 0.5) * 2;
    mouseY = (e.clientY / window.innerHeight - 0.5) * 2;
    
    // 각 이미지를 depth에 따라 이동
    const moveX = mouseX * depth * 50;
    const moveY = mouseY * depth * 50;
});
```

### 자동 위치 배치
```javascript
const positions = ['top-left', 'top-right', ...]; // 8가지
const positionIndex = (slideIndex * 3 + mediaIndex) % 8;
```

### 떠다니는 애니메이션
```css
@keyframes floatAround {
  0%   { transform: translate(0, 0) rotate(0deg); }
  25%  { transform: translate(100px, -100px) rotate(90deg); }
  50%  { transform: translate(0, -200px) rotate(180deg); }
  75%  { transform: translate(-100px, -100px) rotate(270deg); }
  100% { transform: translate(0, 0) rotate(360deg); }
}
```

---

## 🎯 사용 시나리오

### 기업 연말 결산
```javascript
// 매출, 성과, 프로젝트 하이라이트
{ month: 'Q1', stat: '50억', images: [...] }
```

### 프로젝트 회고
```javascript
// 스프린트별 성과
{ month: 'Sprint 1', stat: '15개', video: '...' }
```

### 개인 포트폴리오
```javascript
// 월별 학습 기록
{ month: '1월', title: 'React 학습', stat: '10개' }
```

---

## 🔧 자주 묻는 질문 (FAQ)

### Q1: 이미지는 어디서 구하나요?
**A**: [Unsplash](https://unsplash.com), [Pexels](https://pexels.com) (무료)

### Q2: 동영상은 어떤 형식이어야 하나요?
**A**: MP4 (H.264 코덱), 5MB 이하 권장

### Q3: 12개월이 아니어도 되나요?
**A**: 네! 분기별(4개), 주간(52개) 등 자유롭게 가능

### Q4: 색상을 바꾸려면?
**A**: `style.css`의 `:root` 섹션에서 변경
```css
--primary-color: #YOUR_COLOR;
```

### Q5: 마지막 슬라이드에서 자동으로 넘어가요?
**A**: v2.1부터 클릭해야 넘어갑니다! (사용자 제어권 강화)

---

## 📤 배포 방법

### GitHub Pages (무료)
```bash
git init
git add .
git commit -m "Team year in review"
git push origin main
# Settings → Pages 활성화
```

### Netlify (드래그 앤 드롭)
1. 폴더를 zip으로 압축
2. netlify.com 접속
3. 드래그 앤 드롭

---

## 🎓 핵심 배운 점

1. **라이브러리 선택의 중요성**
   - 커스텀 vs 검증된 라이브러리
   - 유연성 vs 개발 속도

2. **사용자 경험 우선**
   - 자동 vs 수동 제어
   - 피드백의 중요성

3. **점진적 개발**
   - v1.0 기본 → v2.0 기능 추가 → v2.1 개선

4. **문서화의 가치**
   - README, CHANGELOG, 개발과정
   - 미래의 나를 위한 투자

---

## 📞 문제 발생 시

### 슬라이드가 안 움직여요
- 콘솔(F12) 에러 확인
- 인터넷 연결 확인 (CDN)
- 로컬 서버로 실행

### 이미지가 안 보여요
- URL이 올바른지 확인
- HTTPS 사용 (mixed content 방지)
- CORS 정책 확인

### 마지막에서 멈춰요
- **정상입니다!** 클릭하세요 😊

---

## 🎉 완성도

- ✅ **기능**: 100% 완성
- ✅ **반응형**: 모바일/태블릿/데스크톱
- ✅ **문서화**: README + CHANGELOG + 개발과정
- ✅ **커스터마이징**: data.js만 수정
- ✅ **성능**: 최적화 완료

---

**📅 작성일**: 2025-12-25  
**🔖 버전**: v2.1.0  
**⏱️ 개발 시간**: 1일  
**💾 파일 크기**: 84KB

**Happy Coding! 🚀**

